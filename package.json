{
  "name": "saka",
  "description": "A keyboard interface to the web",
  "scripts": {
    "build": "echo \"You must specify the target browser (firefox or chrome). Example: npm run build:firefox\"",
    "build:chrome": "npm run clean && webpack --config webpack.config.js --env=dev:chrome:benchmark --progress --colors",
    "build:firefox": "npm run clean && webpack --config webpack.config.js --env=dev:firefox:benchmark --progress --colors",
    "build:prod": "echo \"You must specify the target browser (firefox or chrome). Example: npm run build:prod:firefox\"",
    "build:prod:chrome": "npm run clean && webpack --config webpack.config.js --env=prod:chrome:nobenchmark --progress --colors && npm run zip-dist",
    "build:prod:firefox": "npm run clean && webpack --config webpack.config.js --env=prod:firefox:nobenchmark --progress --colors && npm run zip-dist",
    "build:benchmark:chrome": "npm run clean && webpack --config webpack.config.js --env=prod:chrome:benchmark --progress --colors && npm run zip-dist",
    "build:benchmark:firefox": "npm run clean && webpack --config webpack.config.js --env=prod:firefox:benchmark --progress --colors && npm run zip-dist",
    "build:profile": "npm run clean && webpack --config webpack.config.js --progress --profile --colors && cp ./static/* ./dist",
    "clean": "rimraf dist",
    "zip-dist": "bestzip dist.zip dist/*",
    "book:dev": "cd book && npm run dev",
    "book:build": "cd book && npm run build",
    "book:publish": "cd book && npm run publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/eejdoowad/saka-bar.git"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/chrome": "^0.0.47",
    "babel-core": "^6.25.0",
    "babel-eslint": "^7.2.3",
    "babel-loader": "^7.1.0",
    "babel-plugin-transform-class-properties": "^6.23.0",
    "babel-plugin-transform-object-rest-spread": "^6.23.0",
    "babel-plugin-transform-react-jsx": "^6.23.0",
    "babili": "^0.1.4",
    "babili-webpack-plugin": "^0.1.2",
    "bestzip": "^1.1.3",
    "copy-webpack-plugin": "^4.0.1",
    "css-loader": "^0.28.4",
    "eslint": "^4.4.1",
    "eslint-config-standard": "^10.2.1",
    "eslint-config-standard-preact": "^1.1.6",
    "eslint-plugin-import": "^2.7.0",
    "eslint-plugin-node": "^5.1.1",
    "eslint-plugin-promise": "^3.5.0",
    "eslint-plugin-react": "^7.2.0",
    "eslint-plugin-standard": "^3.0.1",
    "html-loader": "^0.4.5",
    "markdown-loader": "^2.0.0",
    "rimraf": "^2.5.4",
    "style-loader": "^0.18.2",
    "webpack": "^3.0.0"
  },
  "dependencies": {
    "fuse.js": "^3.0.5",
    "marked": "^0.3.6",
    "material-components-web": "^0.14.0",
    "mosi": "^1.3.0",
    "msgx": "^1.1.3",
    "preact": "^8.2.1",
    "webextension-polyfill": "git+https://github.com/lusakasa/webextension-polyfill.git"
  }
}
